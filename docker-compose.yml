services:
  pagegen:
    build:
      context: .
      target: dev
    ports:
      - "3000:3000"
      - "3001-3020:3001-3020"
      - "5173:5173"
    volumes:
      - .:/app:ro
      - ./data:/app/data
      - ./logs:/app/logs
      - ./projects:/app/projects
    environment:
      - PREVIEW_HOST=0.0.0.0
      - LOG_FORMAT=json
